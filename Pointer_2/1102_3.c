#include<stdio.h>
int main()
{
    int time, num;
    
    scanf("%d",&time);
    int Arr[time];

    for ( int i = 0; i < time; i++)
    {
        scanf("%d",num);
        Arr[i]=num;
    }
    
}